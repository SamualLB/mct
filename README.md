# MCT: Multiple Choice Test Application #

## Running ##
* Use `mvn package` to produce JAR in `targets/`
* Run jar with corresponding executable

### Windows Example ###
```
mvn package
# Test Windows
mct.bat
```

### Unix Example ###
```
mvn package
./mct
```
